Dua perbaikan di `src/routes/print.laporan.$id.tsx`:

1. `.lpd-group` tambahkan `padding-left: 18px` agar A/B/C/D segaris dengan nomor 1, 2, 3.
2. `.lpd-row-inline` kolom pertama: `232px` → `196px` agar `:` C. OUTPUT & D. TINDAK LANJUT segaris dengan `:` baris lainnya.